import { Roboto } from '@next/font/google';

export const fontRoboto400 = Roboto({
  subsets: ['cyrillic'],
  display: 'auto',
  weight: '400',
});

export const fontRoboto700 = Roboto({
  subsets: ['latin'],
  display: 'auto',
  weight: '700',
});
